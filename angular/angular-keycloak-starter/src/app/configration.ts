import {environment} from '../environments/environment';

export class Configuration {
  public static ENV = environment;
  public static APP_NAME = 'Angular Keycloak';
  // public static APP_Version = packageJson.version;
}
